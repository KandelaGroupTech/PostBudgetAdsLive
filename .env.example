# Environment Variables Configuration
# Copy this file to .env.local and fill in your API keys

# Gemini API Key (existing)
VITE_API_KEY=your_gemini_api_key_here

# OpenWeatherMap API Key (FREE - No credit card required)
# Get your free API key at: https://openweathermap.org/api
# Free tier: 1,000 calls/day (60 calls/minute)
# IMPORTANT: Must use VITE_ prefix for client-side access
VITE_OPENWEATHER_API_KEY=your_openweather_api_key_here

# Google Places API Key
# Get your API key at: https://console.cloud.google.com/
# Enable the Places API and Geocoding API
# Free tier: $200/month credit (includes ~28,000 autocomplete requests)
# IMPORTANT: Must use VITE_ prefix for client-side access
VITE_GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# AWS SES Configuration (for backend use only)
# These should be set on your backend server, NOT in the frontend .env
# Get credentials from AWS IAM Console
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_REGION=us-east-1

# Note: The email service requires a backend API endpoint at /api/send-email
# See services/emailService.ts for implementation details
